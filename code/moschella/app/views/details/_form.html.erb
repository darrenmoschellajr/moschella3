<%= form_for(@detail) do |f| %>
  <% if @detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@detail.errors.count, "error") %> prohibited this detail from being saved:</h2>

      <ul>
      <% @detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :bhproduct_number %><br>
    <%= f.number_field :bhproduct_number %>
  </div>
  <div class="field">
    <%= f.label :quantity %><br>
    <%= f.number_field :quantity %>
  </div>
  <div class="field hidden">
    <%= f.label :session_id %><br>
    <%= f.number_field :session_id, :value => params[:session_id]  %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
